import { UtilsChartsPipe } from './utils-chart.pipe';

describe('TimeDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new UtilsChartsPipe();
    expect(pipe).toBeTruthy();
  });

});
